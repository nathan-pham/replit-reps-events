schema: http://localhost:3000/api/graphql
generates:
    ./schema/index.ts:
        plugins:
            - typescript
            - typescript-resolvers
            - typescript-operations
        config:
            fetcher: urql
